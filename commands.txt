# Hide mounted disk from Dock

$ gsettings set org.gnome.shell.extensions.dash-to-dock show-mounts false


# Show mounted disk from dock

$ gsettings set org.gnome.shell.extensions.dash-to-dock show-mounts true


# Hide user folder from Desktop

$ gsettings set org.gnome.shell.extensions.desktop-icons show-home false


# Show user folder from Desktop

$ gsettings set org.gnome.shell.extensions.desktop-icons show-home true


# Add 'Open in Terminator' in nautilus context menu

$ sudo apt install filemanager-actions
$ sudo apt install nautilus-actions

> Filemanager-Actions GUI
 -> Path: /usr/bin/terminator
 -> Parameters: --working-directory=%d/%b
 
 
# Open Terminator in Desktop context menu

gsettings set com.gnome.desktop.default-applications.terminal exec /usr/bin/terminator


# Turn off 'Hotspot Login' window

Go to Settings > Privacy > Connectivity -> Disable Connectivity Checking


# Open URL from Skype into Google Chrome

> Quit skype
$ ln -s ~/.config/mimeapps.list ~/snap/skype/common/.config/mimeapps.list
$ rm -rf ~/snap/skype/common/.config/google-chrome
$ sudo ln -s ~/.config/google-chrome ~/snap/skype/common/.config/


# Hide username and hostname from terminal and show git branch

$ gedit ~/.bashrc

> Modify this area
parse_git_branch() {
 git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'
}

if [ "$color_prompt" = yes ]; then
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;34m\]\w \[\033[01;31m\]$(parse_git_branch)\[\033[00m\]\$ '
else
    PS1='${debian_chroot:+($debian_chroot)}\w $(parse_git_branch)\$ '
fi
unset color_prompt force_color_prompt


# Create Shortcut in Desktop
$ ln -s $PWD ~/Desktop/




